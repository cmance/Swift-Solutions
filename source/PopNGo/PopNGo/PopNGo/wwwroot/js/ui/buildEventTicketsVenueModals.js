
/**
 * Takes the event data and builds a modal that displays information about the venue the event is hosted in
 * @param {any} eventData
 * @returns {any}
 */
export function buildEventVenueModal() {
    return null;
}

/**
 * Takes the event data and builds a modal that shows links to the different ticket options for the event
 * @param {any} eventData
 * @returns {any}
 */
export function buildEventTicketsModal() {
    return null;
}